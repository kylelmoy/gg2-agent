// agentScriptSpare0: empty on purpose. Edit this file directly and rebuild
// with build-fast.js / gg2_rebuild to give a new script real behaviour in
// about 3s, instead of the ~1min build-agent.js needs for a genuinely new
// resource. Once the behaviour is proven, renaming it to its real name still
// needs one full build - but everything up to that point was fast.
